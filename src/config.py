config = {
    "app": {
        "debug": False,
        "flask_host": "0.0.0.0",  # 127.0.0.1 or 0.0.0.0
        "redis_host": "redis",  # can be 127.0.0.1 if locally running server
        "redis_port": 6379,  # this is defualt redis port
        "flask_port": 80,
    },
}
